/** Docentes de un curso (hoy: dutic-mcp). */
export class TeachersSourcePort {
  /** @param {number} courseId @returns {Promise<string[]>} */
  async listCourseTeachers(courseId) {
    throw new Error("not implemented");
  }
}
